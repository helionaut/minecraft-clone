{
  "name": "minecraft-clone",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "generate:inventory-icons": "node scripts/generateInventoryIcons.mjs",
    "build": "tsc --noEmit && vite build",
    "preview": "vite preview",
    "lint": "eslint .",
    "typecheck": "tsc --noEmit",
    "test": "vitest run",
    "test:deploy-smoke": "playwright test --config=playwright.deploy.config.ts",
    "profile:webgpu-startup": "node scripts/runWebGpuStartupProfile.mjs",
    "profile:webgpu-startup:local-preview": "node scripts/runWebGpuStartupProfileLocalPreview.mjs",
    "profile:webgpu-startup:report": "node scripts/summarizeWebGpuStartupProfile.mjs",
    "profile:webgpu-startup:compare": "node scripts/compareWebGpuStartupProfiles.mjs",
    "verify:release-blockers": "playwright test --config=playwright.deploy.config.ts",
    "check": "npm run lint && npm run typecheck && npm run test && npm run build"
  },
  "dependencies": {
    "three": "^0.181.0"
  },
  "devDependencies": {
    "@eslint/js": "^9.38.0",
    "@playwright/test": "^1.58.2",
    "@types/node": "^24.6.1",
    "@types/three": "^0.181.0",
    "eslint": "^9.38.0",
    "globals": "^16.4.0",
    "jsdom": "^29.0.1",
    "typescript": "~5.9.3",
    "typescript-eslint": "^8.46.1",
    "vite": "^8.0.1",
    "vitest": "^3.2.4"
  }
}
